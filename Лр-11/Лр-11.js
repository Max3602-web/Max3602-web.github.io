// Use the first element in the collection
const btn1 = document.getElementsByClassName("myButtonClass")[0];
btn1.addEventListener("click", myFunction);

function buttonHandler(){
    console.log("кнопка работает!")
}
